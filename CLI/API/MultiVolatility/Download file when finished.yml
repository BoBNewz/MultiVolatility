info:
  name: Download file when finished
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:5001/dump-task/<task_id>/download
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
